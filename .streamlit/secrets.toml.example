# Copy to .streamlit/secrets.toml for local overrides (gitignored).
# Environment variables of the same name take precedence over these.
#
# CASH_DATA_SOURCE = "local"          # local | gdrive   (default: gdrive when
#                                     #  gdrive_manifest.json has real IDs, else local)
# CASH_DATA_DIR    = "/Users/you/Projects/CashReports/cash_data/publish"
